services:
  app:
    build: .
    container_name: wc-predict
    ports:
      - "8000:8000"
    volumes:
      - wc-data:/app/worldcup.db
    env_file:
      - .env
    restart: unless-stopped

volumes:
  wc-data:
