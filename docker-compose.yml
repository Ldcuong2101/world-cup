services:
  app:
    build: .
    container_name: wc-predict
    ports:
      - "11111:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/worldcup.db
    env_file:
      - .env
    restart: unless-stopped
